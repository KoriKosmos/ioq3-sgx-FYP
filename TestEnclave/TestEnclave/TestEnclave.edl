enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* Define ECALLs here. */
        public void ecall_generate_random([out] uint32_t* random_number);
    };

    untrusted {
        /* Define OCALLs here. */
    };
};
