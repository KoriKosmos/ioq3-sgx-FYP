enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* Define ECALLs here. */
        public void ecall_calculate_damage(int body_part, [out] int* damage);
    };

    untrusted {
        /* Define OCALLs here. */
    };
};
